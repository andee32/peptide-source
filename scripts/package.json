{
  "name": "@app/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed": "node --env-file-if-exists=../.env --import tsx ./src/seed.ts",
    "seed:subscriptions": "node --env-file-if-exists=../.env --import tsx ./src/seedSubscriptions.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@app/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
